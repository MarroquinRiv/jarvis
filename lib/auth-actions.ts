// Legacy server-side auth actions.
//
// These were used by an earlier auth UI. The project now uses
// client-side Supabase calls in /app/login and /app/register and the
// server-side callback at /api/auth/callback. This file is kept for
// reference but its functions are deprecated and should not be used.

export {};
